# Media inputs

The default teaser needs no downloaded media. `scripts/Titles.swift` creates original typography and gradient plates in `.cache/plates` using the macOS system font. The UI comes exclusively from native Allora recordings.

Place only licensed or original optional audio here, and document its license/source beside it. Pass its path with `--audio`. No audio is bundled by default.
